<UserControl x:Class="SolutionChecker.ReportViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox Grid.Row="1" AcceptsReturn="True" Text="{Binding ReportText}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Right" Grid.Row="2" Height="32" Orientation="Horizontal">
            <Button Margin="2" Command="{Binding SaveAsCommand}" Height="28" Width="80" Content="Save as"/>
            <Button Margin="2" Command="{Binding CopyCommand}"  Height="28" Width="80" Content="Copy"/>
        </StackPanel>
    </Grid>
</UserControl>
